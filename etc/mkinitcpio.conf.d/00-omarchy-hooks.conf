# Omarchy's HOOKS baseline. mkinitcpio sources mkinitcpio.conf and then every
# drop-in here in name order, so the baseline sorts first and later drop-ins
# add their hooks to it instead of being overwritten by it.
HOOKS=(base udev plymouth keyboard autodetect microcode modconf kms keymap consolefont block encrypt filesystems fsck btrfs-overlayfs)
