# The splash shows first boot's progress, so it stays up until first boot hands
# over to account setup or the display manager. Ordering only: on any other
# boot first boot is skipped and this waits for nothing.
[Unit]
After=omarchy-mac-first-boot.service
