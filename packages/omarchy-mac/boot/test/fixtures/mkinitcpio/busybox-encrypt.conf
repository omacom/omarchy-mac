# mkinitcpio.conf of a Mac installed before Omarchy's images: the busybox
# init unlocking the root through the encrypt hook and cryptdevice=.
MODULES=()
BINARIES=()
FILES=()
HOOKS=(base asahi udev autodetect microcode modconf kms keyboard keymap consolefont block encrypt filesystems fsck)
