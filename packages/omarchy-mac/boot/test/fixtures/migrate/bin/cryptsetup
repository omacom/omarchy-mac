#!/bin/bash
fixture=${MIGRATE_FIXTURE:?}
echo "cryptsetup $*" >>"$fixture/pacman.log"
case $1 in
  luksHeaderBackup) echo "LUKS header of $2" >"$4" ;;
  luksUUID) cat "$fixture/luks-uuid" 2>/dev/null ;;
esac
